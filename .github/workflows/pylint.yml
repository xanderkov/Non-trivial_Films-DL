name: Pylint

on: [push]

jobs:
  stage: style
  before_script:
  - pip install -r requirements.txt
  script:
  - ./ci/pylint_runner.sh
  artifacts:
    paths:
      - public/pylint.svg
      - public/pylint.html

